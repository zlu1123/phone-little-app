<view class="login-page">
  <!-- 状态栏占位 -->
  <view style="height: {{ statusBarHeight }}px;"></view>

  <!-- Logo 区域 -->
  <view class="logo-section">
    <view class="logo-wrapper">
      <van-icon name="scan" size="60px" color="#1989fa" />
    </view>
  </view>

  <!-- 登录/注册切换 Tab (暂时隐藏注册入口) -->
  <!--
  <view class="auth-tabs">
    <view
      class="tab-item {{ mode === 'login' ? 'active' : '' }}"
      bindtap="handleSwitchMode"
      data-mode="login"
    >
      <text>登录</text>
      <view class="tab-line" wx:if="{{ mode === 'login' }}"></view>
    </view>
    <view
      class="tab-item {{ mode === 'register' ? 'active' : '' }}"
      bindtap="handleSwitchMode"
      data-mode="register"
    >
      <text>注册</text>
      <view class="tab-line" wx:if="{{ mode === 'register' }}"></view>
    </view>
  </view>
  -->

  <!-- 手机号密码登录 -->
  <view class="login-section">
    <view class="phone-form">
      <van-field
        value="{{ phone }}"
        type="number"
        maxlength="11"
        placeholder="请输入手机号"
        border="{{ false }}"
        bind:change="handlePhoneChange"
        class="input-field"
      >
        <van-icon slot="left-icon" name="phone-o" size="18px" color="#969799" custom-style="margin-right: 16rpx;" />
      </van-field>

      <van-field
        value="{{ password }}"
        type="password"
        maxlength="20"
        placeholder="请输入密码"
        border="{{ false }}"
        bind:change="handlePasswordChange"
        class="input-field"
      >
        <van-icon slot="left-icon" name="lock" size="18px" color="#969799" custom-style="margin-right: 16rpx;" />
      </van-field>

      <van-button
        type="primary"
        block
        round
        loading="{{ isLoading }}"
        disabled="{{ !phone || !password }}"
        bind:click="handlePasswordLogin"
        class="submit-btn"
      >
        登录
      </van-button>
    </view>
  </view>
</view>
